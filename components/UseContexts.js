import { createContext } from "react";
//Lisää tänne kaikki mitä haluat tuoda muihin komponentteihin

const MeasureVal = createContext();

const ModeVal = createContext();

const Themes = createContext();

export {MeasureVal, ModeVal, Themes};